ENVIRONMENT

$ heroku config:set GRAPHITE_HOST=carbon.hostedgraphite.com GRAPHITE_PORT=2003 GRAPHITE_NAMEPREFIX=<<YOUR-API-KEY>>.servers.#hostname#.

DEPLOYMENT

$ heroku deploy:war --war target/cocktail-app-1.0.0-SNAPSHOT.war